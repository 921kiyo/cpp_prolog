#ifndef SOUNDEX_H
#define SOUNDEX_H

#define ENCODE_LENGTH 5;

char[ENCODE_LENGTH] encode(char surname[], char sounde[]);

char encode_translate(char letter);

int compare(char one[], char two[]);

int count(char surname[], char sentence[]);

#endif
